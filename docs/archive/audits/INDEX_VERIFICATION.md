# Convex Index Verification

- Indexes DEFINED in schema.ts: **99**
- Indexes USED in queries: **53**

## ✅ All indexes used in queries are defined in schema.ts

## Unused indexes (defined but no .withIndex call found): 46

Note: 'unused' may include indexes used by Convex internally for
foreign-key cascades or that are simply defined for future use.
These are not a problem — only MISSING indexes cause runtime issues.
