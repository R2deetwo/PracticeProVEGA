You are an expert full-stack developer taking over an existing codebase that has undergone manual modifications. I have a critical, persistent bug that I need fixed once and for all, and I need a complete audit of my CRUD operations.

### THE CORE BUG:
On the "Properties" page, I can successfully CREATE entries. However, when I attempt to DELETE an entry, it fails. The specific behavior is that the app displays a "restored" message instead of successfully deleting the entry. 

### CONTEXT:
- This is an older/modified version of the codebase. Do not assume the code follows standard boilerplate patterns. Follow the actual code as written.
- I need to ensure that EVERY entity in the app that is supposed to support CRUD (Create, Read, Update, Delete) does so flawlessly without state mismatches, rollbacks, or routing errors.

### YOUR EXECUTION PLAN:
Do not just guess and patch one file. I need you to systematically trace the data flow. Execute the following steps:

**PHASE 1: DIAGNOSE THE "RESTORED" BUG (Properties Page)**
1. Find the Delete button/component on the Properties page.
2. Trace the exact function it fires on click.
3. Trace the API endpoint or database mutation it calls.
4. Check for "Soft Delete" implementations (e.g., `is_deleted`, `deleted_at` columns). If it's a soft delete, check if the frontend queries are filtering out deleted items correctly.
5. Check for "Optimistic UI Updates" where the frontend removes it instantly but adds it back (restores) if the backend fails.
6. Identify exactly why the system returns a "restored" message and fix the disconnect between the frontend action and the backend operation.

**PHASE 2: GLOBAL CRUD AUDIT & FIX**
1. Identify all other data entities in the app (e.g., Tenants, Users, Leases, etc., besides Properties).
2. For each entity, verify the 4 CRUD operations:
   - C: Does the form correctly submit and update the state?
   - R: Does the list/view correctly fetch and display data?
   - U: Does editing save correctly and reflect in the UI?
   - D: Does deleting work without the "restored" bug or state rollbacks?
3. Ensure that the delete logic you fixed for Properties is consistently applied to all other entities.

**PHASE 3: CODE IMPLEMENTATION**
1. Apply the fixes.
2. Add clear comments explaining WHY the bug was happening and how the fix resolves it.
3. Do NOT break the existing CREATE functionality on the Properties page while fixing the DELETE functionality.

Start by searching for the Properties page component and the delete handler, and walk me through what you find.