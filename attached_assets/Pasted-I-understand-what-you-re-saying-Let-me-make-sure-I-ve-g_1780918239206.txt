I understand what you're saying. Let me make sure I've got it right:

1. **Bug 1 is fixed** — remove it from the prompt ✅
2. **Send message from unit → should navigate to the Comms tab** (currently it doesn't redirect there)
3. **Rename "Inbox" to "Comms"** — because "messages" is already used elsewhere in the app, so "Comms" avoids confusion
4. **Move the messaging feature out of Revenue Manager and into the Comms/Messages page** — messaging shouldn't live inside Revenue Manager; it should be its own dedicated section under the Comms tab

That's the gist right? Let me deliver the updated prompt:

---

**PROMPT FOR REPLIT — PracticePro VEGA: Unit UX + Rent Range + Visual Status + Matter Fixes + Comms Restructure + ARIA Removal + Vercel Prevention**

---

## PROJECT CONTEXT

I'm building **PracticePro**, a Nigerian legal practice management app with a property management module (Atrium). Stack: **React + Convex**. GitHub repo: `github.com/R2deetwo/PracticeProVEGA`. Convex URL: `https://gregarious-malamute-537.convex.cloud` (env var `VITE_CONVEX_URL`).

---

## BUG 1: UNIT MODAL AND EDIT FORM SHOW PROPERTY-LEVEL FIELDS

**Symptom**: When clicking on a unit or editing a unit, the modal/form shows property-level information alongside unit info. Specifically:
- The unit modal shows "number of units in property" (e.g., 10) — irrelevant when viewing a single unit
- The **unit edit form** shows "number of units: 10" as an **editable field** — this is a property-level field that has no business being in a unit edit form. Editing a unit should only affect that unit's data, not the property's total unit count.

**Root Cause**: The unit modal and edit form are likely reusing the property form/modal component or pulling fields from the parent property object instead of only from the unit object.

**Fix Required**:
1. **Unit VIEW modal** should display **only unit-specific information**: unit name/number, unit type, rent amount, tenant info, lease status, unit size, etc. Remove property-level aggregations (total units count, property-wide stats).
2. **Unit EDIT form** must ONLY contain fields that belong to the unit itself. The following must be **removed from the unit edit form** entirely:
   - Number of units (this is a property attribute, not a unit attribute)
   - Any property-level metadata or aggregations
   - Property name, property address (read-only context at most, never editable from a unit form)
3. The unit edit form should contain ONLY these unit-specific fields:
   - Unit name/number
   - Unit type (residential/commercial)
   - Rent amount
   - Unit size
   - Tenant name
   - Tenant phone
   - Tenant email
   - Tenancy start date
   - Tenancy end date
   - Notes/comments
4. If the same form component is shared between property and unit editing, add a `mode` prop (`"property"` vs `"unit"`) and conditionally render only the relevant fields.
5. The modal heading should say "Edit Unit" (not "Edit Property" or a generic title).

**Files to modify**:
- Unit modal/form component (likely in `src/components/`)
- Any shared form component handling both property and unit editing
- Form schema/field definitions to separate property fields from unit fields

---

## BUG 2: MATTER CREATED FROM UNIT USES PROPERTY INFO INSTEAD OF UNIT INFO

**Symptom**: When creating a matter from a unit, the resulting matter is populated with **property-level information** rather than **unit-specific information**. For example, a matter created from Unit 5 under "No.20 Westsyde Drive" shows the matter title as "Tenancy: No.20 Westsyde Drive" (the property name) instead of referencing the specific unit.

**Root Cause**: The "create matter from unit" flow passes the parent property object to the matter creation function, rather than the specific unit data.

**Fix Required**:
1. When a matter is created from a unit, the matter must be populated with **unit-specific data**:
   - Matter title: "Tenancy: Unit 5 — No.20 Westsyde Drive" (unit first, property as context)
   - Tenant name: the tenant in THAT unit, not a property-level tenant
   - Rent amount: that unit's rent, not an aggregate
   - Lease dates: that unit's start/end dates
   - Unit type: residential or commercial
2. The matter should still reference the property (for context), but primary data comes from the unit.
3. Trace the "create matter from unit" flow and ensure it passes `unit` data, not `property` data, to the matter creation mutation.
4. If the matter schema only has a `propertyId` reference, add a `unitId` field so the matter can be linked back to the specific unit.

**Files to modify**:
- The component/button that triggers matter creation from a unit
- The matter creation function/mutation
- Possibly `convex/schema.ts` to add `unitId` to the matters table
- The matter detail view to display unit-specific info

---

## BUG 3: SEND MESSAGE FROM UNIT DOES NOT NAVIGATE TO COMMS TAB

**Symptom**: When a user sends a message from within a unit (e.g., messaging a tenant from the unit card or unit detail), the message may be sent but the app does not navigate to the messaging/Comms tab. The user is left wondering if the message went through and has no way to see the conversation without manually finding the Comms tab.

**Required Fix**:
1. After sending a message from a unit, the app should **automatically navigate** to the Comms/Messages page (see Feature 5 below for the rename).
2. The newly created conversation should be visible and selected in the Comms page so the user can see their message was sent.
3. If the messaging is currently handled by a modal/popup within the unit view, the flow should be: user clicks "Send Message" → message is created → app navigates to Comms tab with the new conversation open.
4. Ensure the message context (which unit, which tenant, which property) carries over so the conversation in the Comms tab shows the right context.

**Files to modify**:
- The "send message" action handler in the unit card/detail component
- Navigation/routing logic to redirect to the Comms page after sending
- The Comms page component to accept and display the new conversation

---

## FEATURE 1: ADD/REMOVE UNITS FOR EXISTING PROPERTIES

**Requirement**: After a property is initially created, there is no way to add or remove units. Users need this.

**Implementation**:
1. **Add Unit button**: Place an "Add Unit" button prominently in the units tab/list area. Clicking it opens a form (modal or inline) to create a new unit under the current property. Required fields: unit name/number, unit type. Optional: rent, size, notes.
2. **Remove Unit button**: Each unit card/row should have a remove/delete option (icon button or menu item). Clicking it confirms with the user ("Remove Unit 5?") then deletes that unit from the property's units array.
3. The add/remove operations must update Convex via mutations and handle optimistic UI updates correctly.
4. After adding or removing a unit, the units list should update immediately without a full page refresh.

**Backend changes needed**:
- Add Convex mutation `addUnitToProperty(propertyId, unitData)` — appends new unit to property's units array
- Add Convex mutation `removeUnitFromProperty(propertyId, unitId)` — removes specific unit by filtering
- These should be separate mutations, NOT going through the generic `updateItem`

**Files to add/modify**:
- `convex/myFunctions.ts` — add `addUnitToProperty` and `removeUnitFromProperty` mutations
- `convex/schema.ts` — verify units array structure supports these operations
- `src/hooks/useProperties.ts` — add `addUnit` and `removeUnit` hook functions
- `src/contexts/DataProvider.tsx` — add `addUnit` and `removeUnit` actions with correct optimistic updates
- Unit list component — add "Add Unit" button and per-unit "Remove" option

---

## FEATURE 2: RENT RANGE DISPLAY ON PROPERTY CARDS

**Current Behavior**: The property listing shows a single rent number on each property card. But properties have multiple units with varying rents. Showing just one figure is misleading.

**Required Change**: Display a **rent range** in the format **₦X – ₦Y** where X is the lowest unit rent and Y is the highest.

**Display Logic**:
- If `minRent === maxRent` → show `₦{minRent.toLocaleString()}` (no range needed)
- If `minRent !== maxRent` → show `₦{minRent.toLocaleString()} – ₦{maxRent.toLocaleString()}`
- If only one unit → show that unit's rent
- If no units → show "No units" or "—"
- Format numbers with commas (Nigerian Naira standard): 500000 → 500,000
- Use ₦ symbol (Naira), not $

**Files to modify**:
- Property card/list component (wherever the single rent is currently rendered)
- Optionally create a shared utility function `formatRentRange(units)` for reuse

---

## FEATURE 3: UNIT CARD VISUAL STATUS SYSTEM

**Current Behavior**: All unit cards look the same — same color, same style, no visual distinction.

### A. Unit Type Distinction (Base Tint)

| Unit Type | Background Tint | Rationale |
|-----------|----------------|-----------|
| **Residential** | Cool blue-slate (#EFF6FF) | Calm, stable, living spaces |
| **Commercial** | Warm amber-gold (#FFFBEB) | Business, commerce, enterprise |

### B. Data Completeness Status (Left Border Accent)

Each unit card gets a **4px left border** indicating data completeness.

**Critical Fields Hierarchy** (for occupied units):

| Tier | Fields | Why Critical |
|------|--------|-------------|
| **Tier 1 — Must Have** | Tenant name, tenancy start date, tenancy end date | Cannot track who is in the unit or when lease expires — legal/financial risk |
| **Tier 2 — Should Have** | Tenant phone number, rent amount | Critical for communication and financial tracking |
| **Tier 3 — Nice to Have** | Tenant email, unit size, notes | Improves records but not urgent |

**Status Logic**:

```
if unit is VACANT:
    status = "vacant" → grey border (#D1D5DB)

if unit is OCCUPIED:
    if ALL Tier 1 AND Tier 2 fields present:
        status = "complete" → green border (#22C55E)
    else if any Tier 1 field is missing:
        status = "critical" → red border (#EF4444)
    else if Tier 1 complete but any Tier 2 field missing:
        status = "partial" → amber border (#F59E0B)
```

### C. Unit Card Layout

```
┌─────────────────────────────────────┐
│ ▌ Unit 5                    [Type]  │
│ ▌ Tenant: Adeyemi Consulting       │
│ ▌ ₦500,000/yr                      │
│ ▌ Lease: Jan 2025 – Dec 2025       │
│ ▌                                   │
│ ▌ 📞 0802-XXX-XXXX    ✉ email      │
│ ▌                          [Status] │
└─────────────────────────────────────┘
```

- Type badge: "Residential" in blue on blue-tinted bg, "Commercial" in amber on amber-tinted bg
- Contact fields: icon + value if present, **dimmed icon** + "No phone" if missing
- Lease dates: if missing, show "No lease dates" in muted red/amber
- Status badge pill: "Complete" (green), "Needs Info" (amber), "Action Required" (red), "Vacant" (grey)

### D. Implementation Notes

1. Create `getUnitStatus(unit)` → `{ status, missingFields }`
2. Create `getUnitTypeStyle(unitType)` → tint color and badge style
3. Left border: `borderLeft: "4px solid {statusColor}"`
4. Background tint: CSS class `bg-residential` or `bg-commercial`
5. Keep existing card shape/shadows/border-radius — just add tint + left border

**Files to modify**:
- Unit card component (in `src/components/`)
- Add `src/utils/unitStatus.ts` for `getUnitStatus` and `getUnitTypeStyle`

---

## FEATURE 4: REMOVE ARIA INTELLIGENCE SECTION FROM MATTER VIEW

**Current Behavior**: When viewing a matter, there is an "ARIA Intelligence" section with "Draft Agreement", "Generate Status Report", and "Ask ARIA a custom question". Not needed at this stage.

**Required Change**: Remove the entire ARIA Intelligence section from the matter detail view:
1. Remove the "ARIA Intelligence" heading/panel
2. Remove "Draft Agreement" action card
3. Remove "Generate Status Report" action card
4. Remove "Ask ARIA a custom question" input field
5. Remove any related ARIA wrapper/container component
6. The freed space should be reclaimed by the layout naturally
7. Do NOT remove ARIA code from other parts of the app — only from the matter detail view

**Files to modify**:
- Matter detail/view component (likely in `src/components/` or `src/pages/`)
- ARIA Intelligence component imported/rendered in the matter view

---

## FEATURE 5: RENAME "INBOX" TO "COMMS" AND RESTRUCTURE MESSAGING

**Current Behavior**: The app has a messaging feature, but it's confusingly organized:
- The sidebar/tab is called "Inbox" but the app also uses the term "Messages" internally, creating naming confusion
- Messaging functionality currently lives inside Revenue Manager, which doesn't make logical sense — messaging tenants/clients is a communication function, not a revenue function
- When sending a message from a unit (see Bug 3), there's no clear navigation to the messaging section

**Required Changes**:

### A. Rename "Inbox" to "Comms"

1. Rename the sidebar navigation item from "Inbox" to **"Comms"** (short for Communications)
2. The rationale for "Comms" over "Messages": the word "messages" is already used within the app in other contexts (matter messages, system messages, etc.). Using "Comms" creates a clear, distinct identity for this section as the central communications hub. It's also shorter and more modern.
3. Update ALL references: sidebar label, page title, route name, breadcrumbs — everything that says "Inbox" should now say "Comms"
4. If the route is `/inbox`, change it to `/comms`

### B. Move Messaging Out of Revenue Manager Into Comms

1. The messaging feature currently lives inside **Revenue Manager**. This is the wrong place — messaging tenants about rent, lease renewals, or any communication is a communications function, not a revenue function.
2. Move ALL messaging-related UI and functionality out of Revenue Manager and into the **Comms** page
3. The Comms page should become the single, centralized place for all communications: messages to tenants, messages about units, messages about matters, etc.
4. After the move, Revenue Manager should ONLY contain revenue-related features (rent tracking, payment history, revenue charts, etc.) — no messaging
5. If there are shared components or hooks between Revenue Manager messaging and Comms, refactor them so Comms owns the messaging logic and Revenue Manager simply links to Comms when needed

### C. Comms Page Structure

The Comms page should be organized to handle different types of communications:
1. **Conversations list** — shows all conversations (with tenants, about units, about matters)
2. **Conversation view** — shows the message thread for a selected conversation
3. **New message** — ability to start a new conversation (from here OR from a unit/matter context)
4. **Filters** — filter by: tenant, unit, property, matter, unread

**Files to modify**:
- Sidebar navigation component — rename "Inbox" to "Comms"
- Revenue Manager component — remove messaging UI/functionality
- Comms/Messages page component — absorb all messaging features from Revenue Manager
- Routing configuration — update `/inbox` to `/comms` if applicable
- Any references to "Inbox" in labels, titles, breadcrumbs, etc.
- Shared messaging components/hooks — ensure they work from the Comms page

---

## FEATURE 6: EXPAND UNIT CARD IN-PLACE (NOT SCROLL TO TOP)

**Current Behavior**: When a unit card is clicked in the units grid/list, it expands to show more detail but the page scrolls/jumps to the top. This is disorienting, especially with many units — the user loses their place.

**Required Change**: When a unit card is clicked and expands, it should expand **in-place** within the column/row where it already is. The page should NOT scroll to the top.

**Implementation**:
1. Remove any `scrollTo(0, 0)`, `scrollIntoView()`, or `window.scrollTo()` calls that fire when a unit card is expanded
2. If the expanded unit detail is rendered as a separate section/panel at the top, change it to render **inline** — the expanded detail replaces the compact card in the same grid position
3. Use accordion-style expansion: selected card expands in-place, other cards shift down naturally
4. If the current implementation uses a separate "detail panel" at the top, refactor to a per-card expandable section
5. Consider a CSS transition for smooth height change
6. Clicking again or clicking another unit collapses the expanded card

**Files to modify**:
- Units grid/list component (where unit cards are rendered)
- Unit card component (add expand/collapse logic)
- Any scroll-to-top logic triggered by unit selection

---

## FEATURE 7: "MORE ACTIONS" MENU ON UNIT CARDS

**Current Behavior**: Each unit card shows limited actions (just "Edit"). Users need access to more actions without cluttering the card.

**Required Change**: Add a **"More Actions" dropdown menu** (a "⋮" button) to each unit card.

**Menu Items**:
1. **Edit** — opens the unit edit form
2. **Remove Unit** — removes this unit (with confirmation)
3. **Create Matter** — creates a legal matter from unit data (see Bug 2)
4. **Send Message** — sends a message to the tenant; after sending, navigates to Comms tab (see Bug 3 + Feature 5)
5. **Mark as Vacant / Mark as Occupied** — toggles occupancy status
6. **View Tenant** — navigates to tenant details (if tenant assigned)
7. **Copy Unit Details** — copies key unit info to clipboard

**Implementation**:
1. "⋮" icon button in top-right corner of unit card — subtle, dimmed until hovered
2. Clicking opens a dropdown/popover menu
3. Conditional items based on unit state:
   - "Mark as Vacant" only if occupied, "Mark as Occupied" only if vacant
   - "View Tenant" only if tenant assigned
   - "Remove Unit" in destructive style (red text), always requires confirmation
4. Clicking outside or pressing Escape closes the menu
5. Only one menu open at a time
6. Use existing UI components (shadcn DropdownMenu if available)

**Files to modify**:
- Unit card component — add "⋮" button and dropdown menu
- Add handlers for "Mark as Vacant/Occupied" and "Copy Unit Details"
- "Remove Unit" connects to `removeUnitFromProperty` from Feature 1
- "Create Matter" uses unit data per Bug 2
- "Send Message" navigates to Comms per Bug 3 + Feature 5

---

## CRITICAL: VERCEL BUILD PREVENTION

**⚠️ DO NOT regenerate `package-lock.json` using `npm install` inside Replit.** Replit's npm proxy bakes internal URLs into the lockfile that break Vercel builds.

**The Problem**: Replit routes npm through `http://package-firewall.replit.local/npm/...`. Vercel cannot resolve this and the build fails with `ENOTFOUND`.

**Prevention Rules**:
1. **Never run `npm install`** for adding packages. Manually add to `package.json` instead.
2. If you MUST run `npm install`, immediately run:
   ```bash
   sed -i 's|http://package-firewall.replit.local/npm/|https://registry.npmjs.org/|g' package-lock.json
   ```
3. Before any `git push`, verify:
   ```bash
   grep -c "package-firewall.replit.local" package-lock.json
   ```
   If count > 0, run the `sed` command.

---

## EXECUTION ORDER

1. **Bug 1** (unit modal + edit form showing property-level fields like "number of units: 10")
2. **Bug 2** (matter uses property info instead of unit info)
3. **Bug 3** (send message from unit doesn't navigate to Comms)
4. **Feature 5** (rename Inbox → Comms, move messaging out of Revenue Manager) — must be done before Bug 3 works correctly
5. **Feature 6** (expand unit card in-place)
6. **Feature 7** ("More Actions" menu on unit cards)
7. **Feature 4** (remove ARIA Intelligence section)
8. **Feature 1** (add/remove units) — "Remove Unit" from Feature 7 connects here
9. **Feature 2** (rent range display)
10. **Feature 3** (visual status system)
11. **Verify Vercel safety** — run the `grep` check before any git push

---

## TECHNICAL NOTES

- Convex env variable is `VITE_CONVEX_URL` (NOT `VITE_CONVEX_UI`)
- Use functional state updaters `setState(prev => ...)` everywhere in DataProvider to avoid stale state captures
- For add/remove unit mutations, prefer dedicated mutations over generic `updateItem`
- All currency in Naira (₦), format with `toLocaleString()`
- Visual status colors should be subtle — professional dashboard, not loud
- When creating a matter from a unit, pass **unit data** (not property data)
- Consider adding `unitId` field to matters schema for proper unit-to-matter linking
- Unit card expansion must be in-place (accordion-style), NOT scroll-to-top
- The "More Actions" menu should use existing UI components (shadcn DropdownMenu if available)
- The unit edit form must NOT contain property-level fields like "number of units"
- "Comms" replaces "Inbox" everywhere — sidebar, routes, titles, breadcrumbs
- Messaging functionality moves FROM Revenue Manager TO Comms page
- After sending a message from a unit, auto-navigate to Comms tab with the conversation open
- Test each fix independently before moving to the next