Initialize in AGENT MODE. Use @Codebase to audit product entry routers, workspace access controllers, context layouts, pricing matrices, and public landing layouts.

This is a standalone, fresh set of tasks focused on product rebranding alignment, business model optimization, legal document separation, and a critical feature gap implementation. Present a step-by-step execution plan for these 6 tasks:

---

### Task 1: Rebranding & Copywriting Alignment for Pricing Tiers
- **Symptom 1 (Incorrect Trial CTA):** The "Starter" plan CTA incorrectly displays "Start Free". It must not promise a free trial.
- **Symptom 2 (Non-Standard Periodicity):** The subscription pricing displays a non-standard `/yr` or `pa.` label.
- **Action Required:**
  - Locate the pricing tier data arrays. Update the Starter plan's CTA text from "Start Free" to **"Get Started"**.
  - Change all subscription billing frequency text suffixes from `/yr` or `pa.` to explicitly read **"Per Annum"** (or dynamically fall back to **"p.a."** via CSS/media-queries if constrained on narrow mobile viewports).

---

### Task 2: Pricing Logic Consolidation & Feature Gap Validation
- **Problem Statement:** The current pricing matrix overcomplicates differentiation by capping users, managed units, property counts, and active tenant profiles simultaneously. Furthermore, there is a mismatch between what our marketing cards state and what the app structurally handles.
- **Action Required:**
  - Remove user-count limitations as a metric across tiers; multi-user workspace access must be collaborative by default across the ecosystem.
  - Simplify the pricing metrics down to a clean, singular denominator: **Total Tenant Capacity** (scaled systematically by active tenant record blocks).
  - Ensure that any plan packaging the "Revenue Monitor" module automatically unlocks unmetered/baseline automated WhatsApp rent demand notices as a standard native tool capability.
  - **Audit & Match:** Conduct a rigorous alignment sweep between backend capacities and frontend marketing copy. Temporarily hide or strip any listed features on the pricing cards (e.g., automated third-party escrow settlement if unbuilt) that the current codebase lacks the physical state architecture to perform.

---

### Task 3: Main Product Entry Portal UX Enhancement
- **Symptom:** The short master landing split-gate page (`practice-pro-vega.vercel.app`) asking the user to choose their product vector ("Enter Vega" for Law Firms / "Enter Atrium" for Property Managers) triggers erratically across different devices, responding awkwardly to touch states.
- **Action Required:**
  - Refactor the entry action event handlers on the split cards. Change the wrapper event execution logic to fire strictly **on Click** (`onClick`) rather than firing or dragging on casual touch movements (`onTouchStart`) to prevent accidental misrouting on mobile viewports.

---

### Task 4: Public Footer Link Restoration & Screen Striation Fix
- **Symptom 1 (Dead Links):** The global marketing footers across Atrium and Vega feature broken or dead text links for "Features", "Pricing", and "Changelog".
- **Symptom 2 (Layout Striations):** The Resources & Documentation list view displays severe vertical/horizontal layout lines, jagged edges, and overlapping grid artifacts cutting through cards.
- **Action Required:**
  - Map explicit relative paths or anchor tags onto footer navigational elements to ensure they route cleanly to `/pricing` or specific section targets.
  - Audit the Resource card list wrappers. Remove any broken background-clip combinations, collapsing layout boundaries, or overlapping absolute layers causing the visual striations.

---

### Task 5: Multi-Product Legal Document Separation & Nomenclature
- **Problem Statement:** The legal documentation route is monolithic and contains critical brand misnomers.
- **Action Required:**
  - **Bifurcate Agreements:** Separate the public policies into product-specific documents (Atrium Property OS vs. Vega), while preserving their master parent routing back to *PracticePro Legal Technologies Limited*.
  - **Refine Section 3.1 Data Capture Copy:** Revise data intake descriptions from severe language to balanced, context-appropriate phrasing regarding compliance verification requirements for property/firm setup.
  - **Enforce Correct AI Branding Definitions:** Audit the definitions section globally to eliminate bad transcription placeholders. Ensure the following acronyms are explicitly codified:
    - **ALOA:** *Advanced Legal Office Assistant*
    - **Aria:** *Advanced Revenue Intelligence Agent*

---

### Task 6: Role-Based Access Control (RBAC) – Matter Privacy Isolation
- **Problem Statement:** The platform documentation claims that users can flag sensitive litigation files or matters as "Private" to isolate them, but the structural permission layer does not yet exist.
- **Action Required:**
  - Append an explicit boolean `isPrivate` attribute to the database schema for the Matter entity model.
  - Implement a rigorous data-fetch interceptor check on the backend query layer. If `isPrivate === true`, explicitly filter the record stream so it returns *only* to user profiles whose unique IDs are explicitly listed in the matter's assigned team array or creator parameters.
  - Bind an elegant toggle switch on the frontend Matter settings layout screen to manage this permission state smoothly.