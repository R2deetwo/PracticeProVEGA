# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local
.env.*.local
# Environment variables
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

venv/codebase.zip
codebase.zip
upload/
.next/
tool-results/

# Android build artifacts (keep source, ignore builds)
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/app/release/
*.apk
*.aab
*.keystore
# Exception: commit the shared debug keystore so CI builds use the same
# signature as local builds. This prevents "App not installed as package
# appears to be invalid" errors when updating the APK.
!android/app/debug.keystore
